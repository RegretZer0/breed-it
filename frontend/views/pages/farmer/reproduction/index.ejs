<%- include("../../../partials/farmer/farmer_nav") %>

<div class="app-root">
  <main class="container-fluid py-3 py-lg-4 reproduction-shell repro-main">

    <div class="repro-nav-spacer" aria-hidden="true"></div>

    <!-- STACKED FLOW: HERO -> FILTER -> LIST -> MODAL -->
    <div class="repro-stack d-flex flex-column gap-3">
      <%- include("./hero") %>
      <%- include("./filter") %>
      <%- include("./list") %>
    </div>

    <%- include("./modal") %>

    <!-- LEGACY IDs (DO NOT REMOVE) -->
    <section class="d-none" aria-hidden="true">
      <select id="pigletSelect">
        <option value="">-- Loading Piglets... --</option>
      </select>

      <table>
        <tbody id="pigletMonitoringBody"></tbody>
        <tbody id="aiTableBody"></tbody>
        <tbody id="breedingMortalityTableBody"></tbody>
        <tbody id="morphTableBody"></tbody>
        <tbody id="selectionTableBody"></tbody>
      </table>

      <div id="deformityList"></div>
    </section>

  </main>
</div>

<%- include("../../../partials/farmer/farmer_footer") %>