<!-- =========================
     PAGE HERO (STACKED)
========================== -->
<section class="repro-hero card border-0 shadow-sm">
  <div class="card-body">

    <div class="d-flex align-items-start justify-content-between gap-3 flex-wrap">
      <div class="min-w-0">
        <div class="d-flex align-items-center gap-2">
          <div class="repro-hero-icon flex-shrink-0">
            <i class="bi bi-diagram-3"></i>
          </div>
          <div class="min-w-0">
            <h4 class="repro-title mb-1">Reproduction Monitoring</h4>
            <p class="repro-sub mb-0">
              Manage the lifecycle from Insemination → Monitoring (Day 1–30) → Weaning → Selection.
            </p>
          </div>
        </div>
      </div>

      <span class="badge rounded-pill repro-mvp-badge">
        <i class="bi bi-lightning-charge me-1"></i>MVP
      </span>
    </div>

    <!-- Search stays here (same IDs) -->
    <div class="repro-search mt-3">
      <div class="input-group input-group-sm">
        <span class="input-group-text bg-white">
          <i class="bi bi-search"></i>
        </span>
        <input
          type="text"
          id="reproductionSearch"
          class="form-control"
          placeholder="Search by Swine Tag / Stage / Status..."
          autocomplete="off"
        />
        <button class="btn btn-outline-success" type="button" id="reproSearchClearBtn" title="Clear">
          <i class="bi bi-x-lg"></i>
        </button>
      </div>

      <div class="repro-search-hint small text-muted mt-2">
        <i class="bi bi-info-circle me-1"></i>
        Tip: Use quick tabs for stages, then refine with filters.
      </div>
    </div>

  </div>
</section>